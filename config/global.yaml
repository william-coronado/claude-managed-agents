anthropic_api_key: ""          # Fallback only; env var ANTHROPIC_API_KEY takes precedence
default_model: "claude-sonnet-5"
environments_config: "config/environments.yaml"
agents_config: "config/agents.yaml"
